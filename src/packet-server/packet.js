function Packet() {
    this.packetId = 0xFF;
    this.packetName = "none";
    this.decoded = false;
    this.encoded = false;
};

Packet.prototype.encode = function(buf) {  
};

Packet.prototype.encode = function(buf) {
};

module.exports = Packet;
